// Base URL for the marketing site (the prigeex-web repo). This dApp has no
// "/" landing route of its own - the nav brand links out here instead.
export const WEB_URL = process.env.NEXT_PUBLIC_WEB_URL || "https://prigeex.com";
